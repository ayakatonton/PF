<div class="container mt-4">
  <div class="row">
    <div class="col-md-8 mx-auto text-cente ">
      <p class="mi">選択写真</p>
      <div class="row">
        <div class="col-md-8 mx-auto text-center ">
          <p class="kase"><%= @event.group.name %></p>
          <p class="kase"><%= @event.title %></p>
          <p class="kase"><%= @event.event_date %></p>
        </div>
      </div>
       <div class="row">
        <div class="col-md-8 mx-auto text-center ">
          <%= image_tag @photo.image, size:"250x200" %>
        </div>
      </div>
    <!-- 写真の削除 -->
      <div class="col-md-8 mx-auto ">
        <%= link_to '写真を削除する', admin_photo_path(@photo), method: :delete,class: " btn-stitch", data: { confirm: '本当に削除しますか？' } %>
        <%= link_to '写真一覧',admin_photos_path, class: "btn-stitch" %> 
      </div>
    </div>
  </div>
</div>